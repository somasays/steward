"""Alembic environment for the queue schema.

`steward_queue.migrate` is the programmatic entry point (`upgrade_to_head`);
`alembic.ini` at the package root is the same environment for CLI work
(authoring a new revision).
"""
