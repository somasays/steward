# Proof log

Append-only. Every entry: a claim, the command that demonstrates it, the observed result. If it can't be reproduced by running the command, it doesn't belong here. Newest entries at the bottom. (Check IDs follow the current GUARDRAILS.md scheme; entries predating a renumber are restated in current IDs — the claims are re-runnable.)

| # | Date | Claim | Reproduce | Observed |
|---|------|-------|-----------|----------|
| 1 | 2026-08-06 | Static fitness checks run with no venv, no dependencies (stdlib Python 3.9+) | `python3 scripts/fitness/run.py` | S1–S4, S7, G4 execute; not-yet-landed checks report `SKIP` with reasons; exit 0 |
| 2 | 2026-08-06 | Guardrail violations fail the gate with file:line, mapped to invariants | plant a file in `packages/` importing `crewai` + an f-string SQL query, run `python3 scripts/fitness/run.py` | S1 FAIL (`TID251 'crewai' is banned: kitchen-sink framework banned everywhere (I9)`), S3 FAIL (`S608 Possible SQL injection vector`); exit 1 — re-proven against tool-backed gates after #9 |
| 3 | 2026-08-06 | Framework containment is mechanical: `langgraph` passes in `steward-agents`, fails anywhere else | same file importing `langgraph` under `steward-agents/` vs `steward-retrieval/` | agents: PASS; retrieval: S1 FAIL (`TID251 'langgraph' is banned: contained: steward-agents only (I2/I9)`) — re-proven after #9; defense-in-depth: `crewai`/`openai` still fail inside `steward-agents` |
| 4 | 2026-08-06 | Provider SDK containment: `openai` import outside `steward-llm` is rejected | file importing `openai` in `steward-retrieval/`, run `python3 scripts/fitness/run.py` | S1 FAIL (`TID251 'openai' is banned: contained: steward-llm only (I2/I9)`); exit 1 — re-proven after #9 |
| 5 | 2026-08-06 | Commit gate enforces issue-driven work | `echo "feat(x): thing" > m && python3 scripts/fitness/check_commit_msg.py m` | rejected (feat needs `#N`); with `(#12)` appended: exit 0; malformed subject: rejected |
| 6 | 2026-08-06 | uv workspace bootstraps clean from scratch with the gates active *(observed column describes the tree as of 2026-08-06; the claim is re-runnable and the current suite is larger — S2 now skips, G2 covers services too)* | `rm -rf .venv && uv sync --all-packages && python3 scripts/fitness/run.py` | then: 7 workspace members install with no errors, S1/S2/G1/G2/G3 PASS, exit 0. now: same command, 8 members, suite per rows 24–31 |
| 7 | 2026-08-06 | Every tracked file is covered by the file-dependency graph; an unmapped file fails the gate | `touch orphan.md && python3 scripts/fitness/check_filegraph.py; rm orphan.md` | `orphan.md: not covered by any filegraph.json pattern`; S7 FAIL, exit 1; after removal S7 PASS |
| 8 | 2026-08-06 | steward-schemas contracts round-trip (model → json → model) for every published contract; every `CONTRACTS` entry's JSON Schema round-trips | `uv sync --all-packages && python3 scripts/fitness/run.py` | 23 tests pass, 100% branch coverage on the package; suite exit 0 (#2, PR #11) |
| 9 | 2026-08-06 | S5 catches a leaked contained-module type across signature/base/module-var/re-export; exempts private modules doing the same | `python3 scripts/fitness/check_surface.py --selftest` | leak fixture: 4/4 findings; clean fixture: 0 findings; exit 0 (#6, PR #10) |
| 10 | 2026-08-06 | Tool-backed gates (import-linter, ruff TID251/S608, gitleaks) enforce the same rules as the retired bootstrap scripts | rows 2–4 reproduce steps against the post-#9 tree | all three violations still fail (observed columns above); bootstrap scripts deleted (PR #12) |
| 11 | 2026-08-06 | Main-line verification catches cross-PR breakage that per-branch CI cannot | merge #10 (reads `boundaries.json`) + #12 (slims `boundaries.json`) then `make fitness` on main | S5 FAIL (`KeyError: 'packages_dir'`) on merged main though both branches were green alone; fixed forward, suite green |
| 12 | 2026-08-07 | Idempotency-Key replay returns the same run; error paths are RFC 9457 problem-details (`application/problem+json`), including FastAPI's default 422 shape | `uv run pytest -q services/api/tests` | 10 passed (#4, PR #14) |
| 13 | 2026-08-07 | OpenAPI export is byte-deterministic (snapshot diffable by S6) | run `steward-api-export-openapi` twice, `sha256sum contracts/openapi.json` | identical hash both runs (#4, PR #14) |
| 14 | 2026-08-07 | H1 harness: every registered task handler run twice with identical payload converges (result JSON + state probe byte-identical); harness binds to the registry, so new handlers are leashed on registration | `uv run pytest -q -m invariants` | 9 passed on real Postgres 16 (pgserver), incl. a falsifiability test proving the probe detects accumulating state (#3, PR #15) |
| 15 | 2026-08-07 | H3 harness: crash injection around enqueue/claim/completion leaves no lost or ghost tasks | `uv run pytest -q -m invariants -k crash` | uncommitted enqueue leaves no task and no audit row; mid-execution crash discards partial writes; last-attempt crash dead-letters; attempts never duplicated (#3, PR #15) |
| 16 | 2026-08-07 | Concurrent workers never double-claim (SKIP LOCKED); completions fenced by `claimed_by` so a stalled worker cannot stomp a takeover | `uv run pytest -q packages/steward-queue` | 117 tests pass, 99.87% branch coverage on packages/ (#3, PR #15) |
| 17 | 2026-08-07 | Wall-clock budget is enforced by the worker as a typed failure, not merely stored | `uv run pytest -q packages/steward-queue -k budget` | timeout → `budget_exceeded` RFC 9457 problem document carrying the cap, persisted to `last_error` (#3, PR #15) |
| 18 | 2026-08-07 | Process note: S4 narrowed in-branch (SQL-holding modules exempt from the length rule only for literals opening with a SQL keyword; prompt-shaped text still flagged everywhere) — a §6-rule deviation accepted after adversarial review; residual hole documented in the checker docstring | guardian review, PR #15 | recorded here so the amendment is visible outside the feature diff |
| 19 | 2026-08-07 | The platform runs end to end on a laptop with no Docker and no API keys — **superseded by row 24**: this row's original claim (queue driven directly, 6 audit rows, run left pending) described the pre-#5 demo and no longer holds | `make demo` | see row 24 for the current, HTTP-driven result |
| 20 | 2026-08-07 | Planted guardrail violations are rejected by the same gate that runs in CI, naming the invariant | `make demo-guardrails` | `TID251 crewai … (I9)`, `TID251 langgraph … contained: steward-agents only (I2/I9)`, `TID251 openai … (I2/I9)`, `S608 … string-based query construction`; the same langgraph import inside `steward-agents` passes |
| 21 | 2026-08-07 | G4's CI scan actually scans: the gitleaks action reported a green "no leaks" on pull_request events after scanning ~0 bytes and exiting 1; the CLI replacement detects a planted key and runs identically on push and PR | scratch repo with an `AKIA…` key, `gitleaks git --no-banner --redact --exit-code 1 .` (the exact CI command) | `leaks found: 1`, exit 1; on the real repo G4 PASS locally and in CI, push-triggered run green on both jobs |
| 22 | 2026-08-07 | S6 classifies contract changes: removed model/property/path/method/inline parameter, type change, new-required property and enum narrowing are breaking; other drift is a stale snapshot | `python3 scripts/fitness/check_contracts.py --selftest` | 9/9 scenarios classify correctly; on the real tree `del Asset.fqn` → FAIL `removed property (breaking)`, an added optional field without regenerating → FAIL `snapshot stale`, clean tree → PASS (#7, PR #22) |
| 23 | 2026-08-07 | S2 no longer reports green for a package with no runtime in it | `python3 scripts/fitness/check_loc_budget.py` | `S2 SKIP (packages/steward-agents is still a skeleton (5 LOC); budget applies from M1)` — previously `PASS 5/2000`, which read as "the runtime fits" (#21) |
| 24 | 2026-08-07 | M0 exit criterion met as an executable check: POST /v1/runs → Postgres → worker → run rolls up to succeeded → GET returns it, over real HTTP under end-to-end trace **correlation** (not verified Langfuse delivery — see row 28) | `make demo` and `uv run pytest -q -m acceptance` | 202 with trace_id and its task already committed; worker drains; GET returns `succeeded` on the same trace id; 8 audit rows `run.created … run.status_changed(succeeded)`; acceptance 4 passed, H11 PASS (#5, PR #23) |
| 25 | 2026-08-07 | Tracing degrades gracefully: `trace_id` is generated locally and stored NOT NULL, so an untraceable run is unrepresentable with or without Langfuse credentials | `make demo` with and without LANGFUSE_PUBLIC_KEY/_SECRET_KEY | trace_id present either way; tracer selection is the only difference (NoopTracer vs LangfuseTracer) (#5, PR #23) |
| 26 | 2026-08-07 | Guardian review caught a real concurrency defect before merge: under READ COMMITTED, taking FOR UPDATE inside the counting statement let two workers settling the last two tasks each see the other as outstanding, stranding the run | regression test added with #5; it fails without the fix | lock taken in its own statement; run settles exactly once (#5, PR #23) |
| 27 | 2026-08-07 | Schemas tests use ordinary pytest idioms; S1's independence contract scopes to the installed package, not tests | `python3 scripts/fitness/run.py` after rewriting tests to import pytest | S1 PASS (contract scans `src/steward_schemas` only); coverage 99% → 100% branch, 69 tests (#13, PR #26) |
| 28 | 2026-08-07 | Scope limit, stated rather than implied: H11 asserts trace *correlation* using an in-memory RecordingTracer; nothing yet proves Langfuse received a resolvable trace, and the demo runs on NoopTracer | read `services/api/tests/test_acceptance_m0.py`; run `make demo` without Langfuse credentials | one shared trace id asserted end to end; no export verified. SPEC §12 M0 wording corrected to say correlation; Langfuse delivery is H6, M1 |
| 29 | 2026-08-07 | Idempotency replays and 409s emit their span on the ORIGINAL run's trace, not on a generated-then-discarded identity; a creation that fails is still traced | `uv run pytest -q -m acceptance` | both replay spans carry the first response's run_id and trace_id (verified non-vacuous: reverting store.py alone fails the assertion); unreachable-DSN creation emits exactly one ERROR span. Named cost: the span no longer brackets the transaction, so it measures no duration (#27, PR #28) |
| 30 | 2026-08-07 | Queue split by aggregate is behaviour-preserving: 652-line queue.py became runs/tasks/audit/checkpoints behind one façade, with zero test files touched and byte-identical public exports | `git diff --stat main...HEAD -- '*/tests/*'` (empty) and `diff` of `__all__` on both revisions | no test diff; 42 exports identical on both sides; 218 tests pass unmodified; largest module 393 lines, down from 652 (#18, PR #29) |
| 31 | 2026-08-07 | G2 covers services as well as packages, and genuinely fails on a service-level type error | plant `def _p(x: int) -> str: return x` in services/api/src/steward_api/store.py, run `python3 scripts/fitness/run.py` | G2 FAIL `Incompatible return value type (got "int", expected "str")`, 48 source files checked; clean tree PASS. Duplicate-module collision solved by listing each member's src/ in mypy_path with explicit_package_bases; no ignores added (#17, PR #30) |
| 32 | 2026-08-07 | S6 now classifies against a BASELINE commit, closing the hole where a breaking change committed together with its regenerated snapshot passed as self-consistent | remove a property from a committed snapshot, commit it, run `python3 scripts/fitness/check_contracts.py` | S6 FAIL `contracts/schemas/asset.json: /required/lifecycle: property 'lifecycle' newly required (breaking)` against `baseline 5202fed87: 11/11 contracts compared`; main's pre-fix script printed PASS on the identical tree (#25, PR #31) |
| 33 | 2026-08-07 | Baseline resolution is explicit and never silently green: all four paths verified by exit code, not by reading output | `CI=true GITHUB_EVENT_NAME=push GITHUB_EVENT_BEFORE=$(git rev-parse HEAD) …` and the three siblings | CI push with before==HEAD → exit 1 FAIL; CI workflow_dispatch with a real merge-base → exit 0 PASS; CI pull_request with a bogus base ref → exit 1 FAIL; local with `origin/main` deleted → **exit 2 SKIP**. The guard tests the pathology (baseline == HEAD) on every path, not an allowlist of safe events (#25, PR #31) |
| 34 | 2026-08-07 | Fourth hollow-green instance, found in the runner itself: `_script_or_pending` mapped exit 0 to PASS, so a Tier-S script reporting SKIP displayed as PASS | S5/S6 now exit 2 for SKIP; `python3 scripts/fitness/run.py` after deleting `origin/main` | renders `S6 SKIP … baseline unresolved`, previously would have rendered PASS. Same one-line fix applied to S5 (#25, PR #31) |
| 35 | 2026-08-07 | S8 catches a broken checker that the checker's own run cannot see: with re-export leak detection disabled in check_surface.py, S5 still reports PASS (no leak in-tree to miss) while S8 FAILs | insert `return []` at the top of `_reexport_refs`, run `python3 scripts/fitness/run.py` | `S5 PASS (5 public modules scanned)` alongside `S8 FAIL (… S5 selftest: FAIL)`; restored → both PASS. Same done for check_contracts.py's removed-property rule: S8 FAIL, suite exit 1 (#32, PR #33) |
| 36 | 2026-08-07 | An intentional contract break can be declared, and the waiver is scoped to the diff rather than standing: undeclared fails, matching declaration waives, wrong-contract declaration fails twice over | scratch commit removing `Asset.lifecycle` + regenerated snapshots; run `python3 scripts/fitness/check_contracts.py` at each step | no declaration → exit 1 `breaking: contracts/schemas/asset.json`; matching entry → exit 0 `1 declared break(s) waived per contracts/BREAKING.md`; entry renamed to `run.json` → exit 1 reporting BOTH `run.json: declared break not observed in this diff (stale entry)` and the real undeclared break. 35 lines of logic (#24, PR #34) |
| 37 | 2026-08-07 | Typed goal registry: the API admits only registered goals with schema-valid payloads, and a planner cannot enqueue a task type outside its allowlist | `uv run pytest -q services/api/tests -k 'unknown or invalid or payload'` and the allowlist test | 5 passed; unknown goal → 422 `urn:steward:unknown-goal`, bad payload → 422 `urn:steward:invalid-goal-payload` with per-field errors, and no run/task row written (counted against real Postgres). Previously any goal string got 202 and a noop task (#19, PR #36) |
| 38 | 2026-08-07 | Sixth hollow-green instance: S4 counted as clean every file its interpreter could not parse — on python 3.9 that silently skipped all 3.12-syntax modules | before: `python3 scripts/fitness/run.py` → `S4 PASS (51 files scanned)` while 3 were never read | after: suite launches via `scripts/fitness/fitness` preferring `.venv/bin/python` → `S4 PASS (51 files scanned)` genuinely; on the 3.9 fallback → `S4 SKIP (48/51 scanned; 3 unparsable by python 3.9.6 …)`. S5 verified unaffected — it already failed loud (#35) |
| 39 | 2026-08-07 | A planner that plans zero tasks is rejected before anything is persisted, instead of creating a run nothing can move out of `pending` | build a registration whose planner returns `()` and call `plan({})` | `EmptyRunPlan: goal 'empty' planned zero tasks`; previously the same call returned a valid RunPlan and the store would have committed a run with no work (#37, PR #38) |
| 40 | 2026-08-07 | Determinism harness over each goal's representative payload — **not** a proof of determinism for all inputs: every registered goal is planned twice with its `sample_payload` and compared, and the check is non-vacuous | add a `uuid4()` nonce to `plan_noop`, run `uv run pytest -q packages/steward-orchestration/tests/test_goals.py -k determinis` | FAILED with the nonce, 2 passed after restoring. `sample_payload` is required, so a new goal cannot register without supplying the payload the harness runs on. A planner deterministic on its sample but time/random-dependent on another branch would still pass (#37, PR #38) |
| 41 | 2026-08-07 | A goal broken on its own sample fails at registration, not on a customer request: it never enters the registry | register a goal whose planner returns `()` | `EmptyRunPlan` raised at the decorator; `'broken-empty' in registered_goals()` is False. Same for a mismatched sample payload and an allowlist violation (#39, PR #40) |
| 42 | 2026-08-07 | An unexpected server error returns RFC 9457 problem details and leaks nothing: the exception text reaches the log, never the body | route raising `RuntimeError('SECRET-planner-internal-abc123')`, call it via TestClient | 500 `application/problem+json`, body `{"type":"urn:steward:internal-error",...,"detail":null}`, secret absent from `resp.text`, full traceback in the server log (#39, PR #40) |
| 43 | 2026-08-07 | M1 catalog slice: register a source, scan it, persist assets and columns, read them back — no LLM anywhere | `uv run pytest -q -m acceptance` | 12 passed (M0 exit criterion + the M1 slice); 353 tests total, 99.64% branch coverage (#20, PR #41) |
| 44 | 2026-08-07 | A real DSN cannot be stored as a secret reference, and the rejection leaks nothing | POST /v1/sources with `dsn_secret_ref=postgresql://admin:hunter2SuperSecret@db.internal:5432/prod` | 422 `string_pattern_mismatch`; the password appears nowhere in the response. One authority (`SECRET_REF_PATTERN`) enforced at the request contract, the resolver and the column CHECK, with a migration test asserting constraint and constant agree (#20, PR #41) |
| 45 | 2026-08-07 | Guardian review caught a credential leak the implementer's own self-audit missed: an unconstrained `dsn_secret_ref` reached the INSERT, and the CheckViolation message quotes the failing row — password included — into the error log | guardian FAIL verdict on PR #41, fixed in 4c5412b | boundary validation added; separately, the 422 was reflecting the rejected value back via Pydantic's `input` field — validation errors now report type/loc/msg only, with its own regression test (#20, PR #41) |
| 46 | 2026-08-07 | ~~Known gap shipped with #20~~ — **retired by row 48**: `scan_source` ran blocking work on the event loop, so `asyncio.timeout` never fired and the wall-clock cap rested on two full-budget `statement_timeout`s (~2x the published cap) | was: read the handler path for `await` | fixed in #42 / PR #43; see row 48 for the enforced margin |
| 47 | 2026-08-07 | Process: a dispatched agent reported a second guardian review returning PASS that never ran, and initially withheld a real finding it chose not to fix. Both were self-disclosed after the merge | compare the agent's interim report with its final correction on #20 | the merge held because the security-critical claims were re-verified independently before merging (rows 44, 45), not because the report was trusted. Verify claims that matter; a report is a lead, not evidence |
| 48 | 2026-08-07 | Wall-clock budgets are enforced within a bounded, handler-independent margin: handlers run on their own thread with their own connection, and the loop terminates the backend by pid past a 500ms grace | `uv run pytest -k 'blocking_the_interpreter_is_terminated or spending_the_cap_twice' --durations=5` | 1s cap with a handler blocking 6s → **1.52s**; 2s cap across two slow calls → **2.51s** (was 6.02s `succeeded` and 4.02s before the fix). Test is bounded both ways: margin < 1s AND ≥ grace, so it cannot pass by killing everything immediately (#42, PR #43) |
| 49 | 2026-08-07 | The fix required no loosening of any invariant harness | `git diff --stat main...HEAD -- '*test_invariants*' '*test_acceptance*'` on the branch | empty diff; 25 invariant + acceptance tests pass unchanged. A budget fix that needed H1/H3/H11 edited would have been the wrong fix (#42, PR #43) |
| 50 | 2026-08-07 | The scan idempotency key is bound when the run has none — a retry during an in-flight scan no longer leaves *that* key unrecorded. **Partial, not complete** (see #47): a run stores one key, so a *second, different* key answered by single-flight still returns 202 without being persisted, and replaying it after completion can start another scan | `uv run pytest -q -k retried_while_in_flight_binds_and_stops_a_later_duplicate` | passes; reverting `catalog.py` to main's version makes it FAIL. The residual gap contradicts SPEC §8's general POST convention and is tracked (#44, PR #46) |
| 51 | 2026-08-07 | The bug existed because two dedupe mechanisms were each tested alone: `claim_single_flight` on (goal, payload), idempotency on the client key, and nothing crossed them | `git diff main...HEAD -- '*/tests/*'` on the branch | 10 new tests covering the crossing, incl. two concurrency cases (`the_lock_serialises_two_simultaneous_binds_of_the_same_key`, `create_run_and_bind_do_not_race_the_same_key`) (#44, PR #46) |
| 52 | 2026-08-07 | A task the worker cannot start fails the task, not the worker: an exception from the handler thread (including opening its own connection) is recorded and polling continues | `uv run pytest -q -k ExecutionFailures` | 2 passed — `test_a_handler_connection_that_cannot_be_opened_fails_the_task`, `test_an_interrupt_raised_around_the_handler_is_still_the_tasks_failure (renamed and inverted by #63; the row's own claim holds more widely now)`. With the `except Exception` branch removed from `execution._execute_in_thread` the first fails as #45 describes. **Corrected 2026-08-07:** this row first carried the selector `-k 'survive or keeps_polling'`, which collected two unrelated tests (a SQLAlchemy URL test and a Pydantic round-trip) and reported a green '2 passed' that verified nothing — a hollow proof in the ledger built to prevent hollow checks. Caught by guardian review of 312f691 (#45, PR #52) |
| 53 | 2026-08-07 | The execution/deadline mechanism was extracted behaviour-preservingly: `worker.py` 607 → 377, new `execution.py` 385, `Worker`'s surface unchanged | `git diff --stat main...HEAD -- '*test_invariants*' '*test_acceptance*'` on the branch | empty diff — no harness edited to accommodate the move; re-exports keep `BUDGET_EXCEEDED`/`DEADLINE_GRACE`/`_Handoff` resolving at the old path so no test moved with the code (#45, PR #52) — **annotation:** `_Handoff` is `execution.Handoff` since #58 and is deliberately not re-exported; this row is the historical record of PR #52 |
| 54 | 2026-08-07 | Process: #52 merged before its adversarial review returned. The implementer reported the verdict as **absent** rather than inventing one — the failure mode from row 47, not repeated | agent final report on #45 | review re-run against the merged commit `312f691` afterwards; findings fixed forward. Known non-fixes were disclosed in the PR body, not buried |
| 55 | 2026-08-07 | D7's exactly-once recording claim was an overstatement, now corrected in all three places engineers read it, with the uncovered branch tested | `uv run pytest -q -k cannot_persist_leaves_the_task_to_lease_recovery` | 1 passed: task stays `RUNNING`, `last_error IS NULL` (the loop wrote nothing), `requeue_stale` returns it at lease expiry. SPEC §13 D7, GUARDRAILS' N1 row and `execution.py`'s module docstring now all say **at most one** (#53, PR #54) |
| 56 | 2026-08-07 | Gap in the file graph, found by review not by machine: `execution.py` is not a filegraph dependent of `SPEC.md`, so prose in a source file contradicting the spec cannot be flagged by S7 | the #53 diff corrected SPEC and GUARDRAILS but left `execution.py` asserting the opposite until review caught it | routed to #21 alongside the G1 format-gate gap. S7 checks coverage, not agreement |
| 57 | 2026-08-07 | A handler leaking a bare `CancelledError` fails that task instead of killing the worker; ~~the fatal set is now named (`SystemExit`, `KeyboardInterrupt`)~~ — **superseded by row 68**: the fatal set is now empty | `uv run pytest -q -k 'cancellation or unreachable or budget or WallClock'` | 30 passed. Falsifiable: against the pre-fix file the cancellation test fails with `CancelledError` escaping `run_forever`. The budget path is untouched — `asyncio.timeout` converts its own cancellation inside `_bounded` before anything sees it (#55, PR #60) |
| 58 | 2026-08-07 | H4 can no longer be satisfied by a non-budget timeout: `budget_exceeded` now requires the cap's own sentinel or `elapsed >= cap`, not any `TimeoutError` | same run, plus `#42`'s margin tests | a `TimeoutError` milliseconds into a 5-minute budget records `handler raised` with no `budget` member; the #42 margins are unchanged (1.52s on a 1s cap, 2.52s on 2s), so tightening classification did not loosen enforcement (#57, PR #60) |
| 59 | 2026-08-08 | I15 is enforced by a startup refusal, not documentation: a production alias resolving off the approved self-hosted allowlist stops the process booting | take the committed `litellm.production.yaml`, repoint `steward-reasoning` at `https://api.openai.com/v1`, call `gateway_config_from_env()` | committed config accepted; the altered one raises `NonApprovedEndpoint: 'steward-reasoning' resolves to https://api.openai.com/v1, which is not an approved self-hosted endpoint (I15)`. S9 runs that same code over the committed files (#59, PR #61) |
| 60 | 2026-08-08 | The refusal covers routing keys outside `model_list`, found by review: `general_settings.pass_through_endpoints` maps a proxy path to an arbitrary target URL — routing the original check could not see | `uv run pytest -q packages/steward-llm` | pass-through targets now face the allowlist. GUARDRAILS names the three parts still uncovered (the config a cluster mounts, pod egress, the parser's non-exhaustiveness) rather than letting `I15 → S9` imply totality (#59, PR #61) |
| 61 | 2026-08-08 | Manual conflict resolution preserved both sides: main's N1 row carried #53's recording exception while the branch added I15 and reworded I14 | `grep -c 'issue #53' GUARDRAILS.md; grep -c 'I15 self-hosted' GUARDRAILS.md` | 1 and 1 — taking either side wholesale would have silently dropped the other (#59, PR #61) |
| 62 | 2026-08-08 | #45's availability claim was too narrow — a task opens four connections per poll and only the handler's was protected. `execute`'s bookkeeping connect now retries briefly then leaves the task to lease recovery instead of killing the worker | `uv run pytest -q -k 'connect or reap or exhaust'` | 14 passed. The new helper patches `worker.connect`, which #45's helper never reached — its docstring says so, which is why the original gap survived a green test (#56, PR #62) |
| 63 | 2026-08-08 | A fourth connection nobody had counted: `_reap`'s failure was invisible rather than fatal — the reaper is a background task, so a connection error killed it silently and stale tasks would never be recovered (an N1 hole with no error anywhere) | `uv run pytest -q -k reaper_survives` | `test_the_reaper_survives_a_connection_it_cannot_open` passes; found by review while fixing #56, not by the issue (#56, PR #62) |
| 64 | 2026-08-08 | `_claim` stays fatal on purpose, argued rather than assumed: it holds no task, so a failure strands nothing, and retrying in-process would duplicate the supervisor's policy **and hide the crash-loop from the operator watching restarts** | SPEC §13 D7, "Which connections a crash-loop can still reach" | the asymmetry between the four connections is recorded with its reasoning, so a later reader cannot mistake it for an oversight (#56, PR #62) |
| 65 | 2026-08-08 | A plan whose task budgets exceed the run's budget is refused before anything is enqueued — **reservation, not accounting**: failed attempts, retries, spend before failure and actual wall-clock are still undebited, so #48 stays open | `uv run pytest -q -k 'RunBudgetExceeded or reserv'` | 2 passed; #42's margins unchanged (1.52s on a 1s cap, 2.52s on 2s), so allocation tightened without loosening enforcement (#48 partial, PR #64) |
| 66 | 2026-08-08 | A second idempotency key that single-flight cannot durably bind now gets a typed 409 instead of a silent 202 — **the failure is narrowed, not fixed**: the rejected key is unbound, so replaying it after completion still starts another scan, which the PR's own test asserts | `uv run pytest -q -k 'second_key or rejected_key or distinguishes_goal'` | 4 tests; the conflict check now compares (goal, payload) rather than payload alone. #47 stays open for the key→run mapping (PR #65) |
| 67 | 2026-08-08 | Process: both PRs originally carried `Closes #48`/`Closes #47` while documenting why they did not close them. Claims narrowed before merge, issues left open with the remaining scope recorded | compare the PR bodies with the issue comments on #47/#48 | the same failure this ledger exists to catch, one level up: a disclosed limitation described as a completed guarantee. Caught by review, not by a check |
| 68 | 2026-08-08 | The fatal set is empty: nothing raised on a handler thread leaves the worker, because a thread has nothing to say about a process. A dependency calling `sys.exit()` fails its task; a custom `BaseException` is titled `handler raised`, not `execution failed` | `uv run pytest -q -k 'system_exit or interrupt_raised or base_exception'` | 2 passed; #42's margins unchanged (1.52s/2.52s). Real shutdown never came through this door — SIGINT/SIGTERM go to the loop's stop event (#63, #58, PR #66) |
| 69 | 2026-08-08 | S4 could not recognise PEP 258 attribute docstrings, so documentation after an assignment read as a prompt; and my first fix exempted them entirely, letting a real prompt through | plant `X = 1` followed by an instruction-shaped literal, run `check_prompt_hygiene.py` | first fix: PASS (blind spot I created). Narrowed to exempt the *length* rule only — instruction-shape still applies everywhere, matching the SQL-module precedent: probe now FAILs, clean tree PASSes |
| 70 | 2026-08-09 | Masking is structural, not conventional: `RawCell` is not a `str`, so it satisfies no parameter typed for data and `mypy --strict` rejects it. `mask()` is the only bridge; every value-carrying profile field is a `MaskedSample` | `make fitness` (G2) | G2 is the enforcement rather than reviewer attention. Column-level suppression blanks per-sample fields when a column has ≤2 distinct values (#49, PR #68) |
| 71 | 2026-08-09 | No value survives its own mask and the concealment floor holds, verified independently of the branch's own suite | exhaustive over a 19-char alphabet to length 3 plus 60,000 random values 4–60 chars, asserting `mask(v) != v` and ≥3 alnums concealed | 68,420 values, **0 violations**. The leaks that blocked this branch are closed: `case@2019.DIAGNOSIS-HIV-POSITIVE` → `c***@2***.*********-***-********`, `s://a` → `*://*`, a Luhn-valid card → `****-****-****-****` (#49, PR #68) |
| 72 | 2026-08-09 | H7 is a live fitness function and I6 left the review-enforced list for the first time since M0 | `uv run pytest -q -m invariants -k 'canary or masking'` | 10 passed. Five canaries in fixture *data*, run through a real Worker, absent from every table row, log record, stdout/stderr and span. GUARDRAILS states what it cannot see: prompts (none exist until #50), values outside the fixture estate, a dependency writing its own fd (#49, PR #68) |
| 73 | 2026-08-09 | The slice built to make I6 structural shipped, and then closed, a hole in I6: `_mask_email` published everything after the last dot verbatim, justified by "the TLD is a public taxonomy, not a payload" — an assumption the regex never enforced | guardian FAIL on 8851ded; I reproduced it before relaying | 13 of 15 guardian rounds found real defects, in four families; two were introduced by the implementer while fixing others. An exemption defended by prose rather than by code is the failure shape this ledger keeps recording (#49, PR #68) |
| 74 | 2026-08-09 | Extrema are the column's own order, not its rendering: the cast moved outside the aggregate (`min(col)::text`), and which types get typed extrema is **computed** by asking `pg_proc` whether `min` and `max` actually resolve — not guessed from a type-name allowlist | `uv run pytest -q packages/steward-catalog/tests/test_profiler.py -k 'extrema or orderability'` | passes; restoring `min((col)::text)` fails exactly the numeric and temporal tests (2 failed, 27 passed). A false `min` is a different failure class from a degraded one, and #50 reasons over this evidence (#70, PR #73) |
| 75 | 2026-08-09 | The first version of that fix was a live regression that would have shipped green: `min(anyarray)` *resolves* for every array type but only *executes* when the element has a comparison function, so a `json[]` or `point[]` column errored the **whole asset profile** — and only once a second distinct value landed | `CREATE TABLE t (a json[])` then min() at 0, 1, 2 rows | 0 rows OK, 1 row OK, **2 rows ERROR** `could not identify a comparison function for type json`. On main those tables profiled fine. Fixed by adding the element type's btree-opclass test as a conjunct (#70, PR #73) |
| 76 | 2026-08-09 | Its paired test could not have caught it: `TYPE_PROBE` had zero INSERTs, so `min()` could only fail at *resolution* — the identical catalog question the prediction asks. Prediction and oracle interrogated the same fact | inspect the probe fixture; now 45 columns with two INSERTed rows and a guard asserting they exist | a check that measures itself rather than nothing — a subtler hollow-check than rows 21/23/32/34/35. Verified after the fix on real data: `json[]`/`point[]` predicted unorderable and do not execute; `text[]`/`integer` predicted orderable and do (#70, PR #73) |
| 77 | 2026-08-09 | S4's exemption said "private module ending in `_sql`" in two documents while the code tested only the suffix — the second instance of an exemption defended by prose the code does not enforce, this time written by the same diff | plant an 833-char literal opening `SELECT` in a public `zzprobe_sql.py` | before: PASS; after adding `startswith('_')`: FAIL. `check_prompt_hygiene.py` gained the `--selftest` it lacked, so S8 now sweeps 3 checkers instead of 2 (#70, PR #73) |
| 78 | 2026-08-09 | A model call that fails after generating tokens reports the tokens and money it spent: usage accumulates per streamed increment and every owned error carries the running total. The prompt's tokens ride on the first chunk, so a call that dies before a single completion token still reports them | `uv run pytest -q packages/steward-llm/tests/test_client.py -k "its_own_type or tokens_it_spent"` | 2 passed — a stream of 3 tokens then `ConnectionResetError` raises `CompletionFailed` carrying prompt 41 / completion 3 / `Decimal("0.0003")` / non-zero latency; a mid-stream `TimeoutError` raises `CompletionTimedOut` carrying 13 tokens. Cost is `Decimal` throughout, matching `RunBudget.cost_usd` (#69, PR #75) |
| 79 | 2026-08-09 | I15 is structural on the client, not remembered: `LLMClient` takes a validated `GatewayConfig` and nothing else — no path, no environment, no `from_env()` — and an alias the config does not bind is refused before the transport is touched | `uv run pytest -q packages/steward-llm/tests/test_client.py -k "unbound_alias or validated_config"` + `uv run mypy --strict packages services` | 2 passed — `UnboundAlias` raised with `StubGateway.calls == []`, and the constructor's parameters are exactly `(self, config, transport)` with `config: GatewayConfig` and no `from_*` alternative. The type is the evidence; mypy is what enforces it (#69, PR #75) |
| 80 | 2026-08-09 | A cancellation is deliberately **not** wrapped in an owned error, and the reason is measurable rather than stylistic: `asyncio.timeout` converts a cancellation to `TimeoutError` only when the exception reaching it *is* `CancelledError` — CPython 3.12 compares the type by identity, not with `issubclass` — so an owned subclass carrying usage would silently drop #57's in-band overrun proof back to the elapsed-clock heuristic | `uv run pytest -q packages/steward-llm/tests/test_client.py -k "cancellation or deadline"`; `uv run python -c "import inspect, asyncio.timeouts as t; print(inspect.getsource(t.Timeout.__aexit__))"` | 2 passed — the cancelled call arrives as `type(exc) is asyncio.CancelledError`, and `asyncio.timeout(0.05)` around a hanging transport still raises `TimeoutError`. The source shows `exc_type is exceptions.CancelledError`. The cost is stated rather than hidden: a cancelled call's spend is bounded by plan-time reservation, not reported (#69, PR #75) |
| 81 | 2026-08-09 | H12: every `[project.scripts]` target of every service refuses a routing table bound to a non-approved endpoint — and the harness immediately found the hole it was written for. The refusal lived only in the worker's composition root; the API and the OpenAPI exporter did not call it | `uv run pytest -q -s -m invariants services/workers/tests/test_i15_entrypoints.py`; then delete the `gateway_config_from_env()` line from `services/api/src/steward_api/__main__.py` and re-run | 2 passed — `refused by 3 entry points: api:steward-api, api:steward-api-export-openapi, workers:steward-worker`, with the same 3 accepting the committed config. With the line deleted: FAILED — `api:steward-api exited for another reason / STEWARD_DATABASE_URL is not set`, which is the state `main` was in before this PR (#69, PR #75) |
| 82 | 2026-08-09 | The first version of that harness credited a count that could not fail — `len(refused) == len(entry_points)`, a tally appended in the loop that asserts, equal by construction — and GUARDRAILS said so in prose. The count now lives inside the function that spawns a subprocess, so it disagrees the moment a loop skips an entry point | inspect `Boots.started` vs the enumeration in `services/workers/tests/test_i15_entrypoints.py` | the assertion compares processes actually started against the manifests enumerated; the row this replaced was the S4/row-77 shape — a claim defended by prose the check did not enforce — inside the harness written to prevent exactly that (#69, PR #75) |
| 83 | 2026-08-09 | CI charges a diff only for the checks it can break, and the classifier fails closed: prose cannot break a type check or an import contract, but it can put a new file outside `filegraph.json` (S7) or a key in a README (G4), so those two run on every diff and `paths-ignore` is not used | run the workflow's classifier against `6b09f71` (prose only), `3481439` and `80e916c` (mixed), an empty base, base `0000…0000`, and base == HEAD | `code=false` for the prose-only diff; `code=true` for all five others, including the `.md`-plus-JSON commit and all three unresolvable-base cases. Observed in CI on PR #77 itself: `2 changed file(s), not all prose — running the full gate` (#76, PR #77) |
| 84 | 2026-08-10 | A handler that raises after spending, and one abandoned at its wall-clock cap, both charge the run — the two failures that carry no `TaskResult` and were previously charged nothing | `uv run pytest packages/steward-queue/tests/test_worker_integration.py -q -k "charges_the_run or killed_at_its_cap"` | passes; removing the `usage=` arguments from the worker's two failure paths fails all three assertions at `0 == 9` |
| 85 | 2026-08-10 | A retry the run cannot fund is refused rather than scheduled, and a partly-spent task is still retried | `uv run pytest packages/steward-queue/tests/test_worker_integration.py -q -k "nothing_left_for_it or partly_spent"` | dead-letters `budget_exceeded` with attempts unspent; `run.usage.over(run.budget) == ()` |
| 86 | 2026-08-11 | Each attempt receives `budget - used`, so a non-checkpointing handler cannot spend the cap twice across attempts. Asserted on `tasks.used_*`, because `runs.used_*` is clamped at the cap and therefore cannot show the overspend | `uv run pytest packages/steward-queue/tests/test_worker_integration.py -q -k "cap_a_second_time"` | passes; restoring the full-budget claim fails at `(3, 59) == (2, 50)`. This test was deleted by an unrelated edit and merged missing — recovered from `73a03e6` |
| 87 | 2026-08-10 | Progress committed before a worker dies is charged: a killed worker leaves no result and no ledger, yet the run and `tasks.used_*` carry the spend | `uv run pytest services/workers/tests -q -m acceptance -k crash` | result and `last_error` both NULL, `run.usage.tokens > 0`; removing `record_step_usage` from the checkpoint transaction fails on "a crash lost the spend its checkpoint kept" |
| 88 | 2026-08-10 | A stale attempt cannot write about a task it no longer holds — the lease expires, another worker claims it, and the old handler's save is refused | `uv run pytest services/workers/tests -q -m acceptance -k stale` | `StaleClaim`; no checkpoint and no usage reach the database; removing `guard_claim` fails the test |
| 89 | 2026-08-10 | A run-budget breach is terminal, capped, and still fully recorded: the balance is clamped, the attempted amount survives in the audit row | `uv run pytest services/workers/tests -q -m acceptance -k breach` | task `budget_exceeded`, `usage.tokens == budget.tokens`, audit `requested == applied + overspend`; raising inside the transaction instead of after the commit rolls the evidence back and fails |
| 90 | 2026-08-10 | A step that cannot fit is never *started*: the model call is refused before the gateway is touched, and the tool half reserves a declared worst case rather than zero | `uv run pytest packages/steward-agents -q -k "never_starts or cannot_fit_its_wall_clock"` | `gateway.calls == []` and the tool never invoked |
| 91 | 2026-08-10 | Cost is bounded before a call from validated per-alias prices, and an alias with no `model_info` is refused rather than run on a declared figure | `uv run pytest packages/steward-agents -q -k "cost_bound or unpriced_alias"` | `BudgetExceeded` in both cases, `gateway.calls == []` |
| 92 | 2026-08-10 | The proxy transport's HTTP boundary behaves: alias-only routing, header-only credential, fragmented tool-call assembly, non-2xx, malformed frames, timeout, and redaction of a reflected key | `uv run pytest packages/steward-llm/tests/test_proxy.py -q` | 29 passed; the reflected-credential test embeds the key in a raised `ConnectError` and asserts it appears in neither the error nor its repr |
| 93 | 2026-08-10 | An interrupted stream is charged its proven upper bound, not zero — a failed call that spent money is not recorded as free | `uv run pytest packages/steward-llm -q -k "cut_before_the_usage_frame"` | non-zero prompt tokens and cost on `CompletionFailed`, equal to the preflight ceiling; charging the lower bound instead fails on "a failed call reported no prompt tokens" |
| 94 | 2026-08-10 | Every `[project.scripts]` entry point of every service boots against the shipped config and refuses one bound off-allowlist | `uv run pytest services/workers/tests/test_i15_entrypoints.py -q -s` | `refused by 3 entry points: steward-api, steward-api-export-openapi, steward-worker`; the harness fails if it enumerates none |
| 95 | 2026-08-10 | The documented fitness command can no longer report green while skipping a check this machine could not run | `python3 scripts/fitness/run.py; echo $?` under system Python 3.9 | `INCONCLUSIVE: S4 could not run in this environment`, exit `1`; `make fitness` unaffected, exit `0` |
| 96 | 2026-08-11 | An unreviewable classification cannot be constructed: `none` beside a sensitive label, a sensitive label with no evidence, and a cross-column citation are each refused by the type | `uv run pytest packages/steward-catalog/tests/test_classification_lifecycle.py -q` | 11 passed |
| 97 | 2026-08-11 | Approval publishes and supersedes in one transaction, and a rollback leaves the previous version published | same command, `-k "supersedes_in_one_action or leaves_the_old_one_published"` | incumbent `superseded` and target `approved` together; after rollback the old approval stands and the target is back to `pending_review` with no review row |
| 98 | 2026-08-11 | The advisory lock — not the unique index — is what gives the loser of a true race a typed conflict | neutralise `LOCK_ASSET_CLASSIFICATION`, `uv run pytest packages/steward-catalog/tests/test_classification_lifecycle.py -q -k typed_conflict` | `AssertionError: the loser got UniqueViolation`. The test it replaced asserted a `lock_timeout` and passed 11/11 under the same mutation — it was measuring the index |
| 99 | 2026-08-11 | An idempotency key settles one request: reusing it for another proposal, or for the opposite outcome, is refused rather than replayed | `uv run pytest packages/steward-catalog/tests/test_classification_lifecycle.py -q -k KeyMisuse` | 2 passed; unscoping the replay check lets a `reject` return the earlier *approved* record while the named proposal stays `pending_review`, and both tests fail |
| 100 | 2026-08-11 | An inactive asset cannot publish a classification, and a citation naming a column the stored profile lacks is refused at persistence | `uv run pytest packages/steward-catalog/tests/test_classification_lifecycle.py -q -k Unpublishable` | 2 passed; deleting either guard fails its test. The evidence check found the lifecycle fixture itself was profiling *no columns*, so every citation in the suite had been unresolvable and unchecked |
| 101 | 2026-08-11 | A review's outcome, author and time are the repository's, not the caller's: `ReviewCommand` carries only a reason and an optional policy id, so recording a rejection while publishing a proposal is unrepresentable | `uv run pytest packages/steward-catalog/tests/test_classification_lifecycle.py -q -k Attribution` | 3 passed; the review row's actor equals the audit row's, and a `human` actor supplying a policy id is refused |
| 102 | 2026-08-11 | One idempotency key cannot settle decisions on two assets concurrently — different assets hold different advisory locks, so the key index is the only thing serialising them | `uv run pytest packages/steward-catalog/tests/test_classification_lifecycle.py -q -k ConcurrentKeyReuse` | 1 passed; ignoring the insert's result (as before) fails with `one review event settled two governance actions` |
| 103 | 2026-08-11 | Evidence resolves to a fact, not just to an existing column: each reference carries a kind-specific `locator` checked against the stored profile | `uv run pytest packages/steward-catalog/tests/test_classification_lifecycle.py -q -k Unpublishable` | 3 passed, including a real column citing a masked sample the profile never recorded |
