[project]
name = "steward-workers"
version = "0.1.0"
description = "Steward worker entrypoints (one per agent type). Skeleton only."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/steward_workers"]
