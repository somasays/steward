[project]
name = "steward-api"
version = "0.1.0"
description = "Steward API service (FastAPI gateway). Skeleton only — see issue #4."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/steward_api"]
